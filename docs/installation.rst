.. highlight:: shell

============
Installation
============


Stable release
--------------

To install elen, run this command in your terminal:

.. code-block:: console

    $ pip install elen

This is the preferred method to install elen, as it will always install the most recent stable release. 

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for elen can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/rooterkyberian/elen

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/rooterkyberian/elen/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/rooterkyberian/elen
.. _tarball: https://github.com/rooterkyberian/elen/tarball/master
